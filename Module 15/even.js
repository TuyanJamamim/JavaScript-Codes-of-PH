let num = 1;
while (num <= 10) {
//   console.log(num);
    // num += 2; // Increment by 2 to get even numbers
    if (num % 2 === 0) { // Check if the number is even...add if condition can e used inside a loop
        console.log("Even number:", num);
    }
    num++;
}