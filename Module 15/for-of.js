const numers = [12, 98, 45, 63, 23, 56, 78];
console.log(numers);

for (const num of numers) //I want to get each elements of numers array y num variale
    {
    console.log(num);
}
const fruits = ['apple', 'anana', 'banana', 'orange', 'grape'];
console.log(fruits);
for (const fruit of fruits) //I want to get each elements of fruits array y fruit variale
    {
    console.log(fruit);
    console.log("I want to eat")//this msg will e printed repetatively after each fruit as it is inside the loop
}

