let num = 1;
let sum =0;//for addition purpose variale the initial value will e 0
while (num <= 10) {
    console.log(num);
    sum += num; //this will add the value of num to sum in each iteration
    console.log("The sum is: ", sum); //this will print the sum after each iteration
    //sum = sum + num; //this is another way to add the value of num
    num++;
    
}