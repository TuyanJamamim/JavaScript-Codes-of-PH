var first = 'Tuan';
var second = 'Jamamim';
var fullName = first + ' ' + second; // Concatenating strings with a space in between...putting a space in between the two strings..and putting + is used for concatenation
console.log(fullName); // it will return Tuan Jamamim
console.log('40'+ 2); // Concatenating a number with a string will return a string '402'...it will not add 40 and 2, it will concatenate them..only concatenitation is different....in sustraction,multiplication,division it will convert the string to a number and then perform the operation
console.log('40' - 2); // Subtracting a number from a string will return 38...it will convert the string to a number and then subtract 2 from it
console.log('40' * 2); // Multiplying a number with a string will return 80...it will convert the string to a number and then multiply it by 2..