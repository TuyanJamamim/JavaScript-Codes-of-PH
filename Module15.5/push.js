const ages = [];
const numers = [1, 2, 3, 4, 5];

console.log(numers);
numers.push(44);
numers.push('Tuan')
numers.push('Tuan' , 99)
console.log(numers)//all of the elements will e pushed inside the array..single multiple and different types of values like numers and string can e pushed at the same time
