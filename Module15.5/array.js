//purpopse of array is to put multiple thing at one variale

const numers = [ 12, 22, 23 ] //array structure datatype variale = third bracket and inside third bracket data(num,string) and a coma to seperate them....num and string can e putted inside same array

console.log(numers)

for( num of numers) {
    console.log(num)
}

const names = ['Tuan','Shaki', 'Shuvro', 'Mahin']
console.log(names)
const address = ["Tuan", 3]
console.log(address)