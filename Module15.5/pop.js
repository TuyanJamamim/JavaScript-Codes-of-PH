const friends = ['Tuan','Shanto' , 'Nadim', 'Sajid','Mahin']
console.log(friends);
// friends.push('Niloy');
// console.log(friends);
const out1 = friends.pop();//Removes the last element from an array and returns it. If the array is empty, undefined is returned and the array is not modified.
const out2 = friends.pop()//two last elements een removed from the array

console.log(friends)
console.log(out1 , out2)//here is how two variales can e printed which were extracted from friends variale

