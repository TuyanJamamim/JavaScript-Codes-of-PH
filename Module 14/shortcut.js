const isLeader = true;
if (isLeader === true)//even if === is not used for comparison, it will still work because isLeader is a boolean value true
     {
    console.log("Leader asche");
}
else {
    console.log("Leader nai");
}
 const isPassed = flase;
// isPassed = !isPassed;...updating isPassed to the ooposite value...this is called toggling
if (isPassed === false)// if(!isPassed) and if (isPassed == false) are same ecause here isPassed is a boolean value false
     {
    console.log("You have failed the exam");
}
else {
    console.log("You have passed the exam");
}

//! means false of current value...ut !! means true of current value